{{>header}}

    <h1>Producer Gems</h1>
    {{> menu}}
    <h3>Trending</h3>
    <ul>
      {{# songs}}
        <li><a href="song/{{_id}}">({{upvotes}}) {{title}} - {{artist.name}}</a></li>
      {{/songs}}
    </ul>

{{>footer}}
